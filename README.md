# Retail-Sales-Data-Warehouse
 A production-grade data warehouse that ingests raw transactional data, cleans it, models it into a dimensional schema, runs an automated ETL pipeline, and exposes analytical views for business intelligence.
